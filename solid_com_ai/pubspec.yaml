description: "SolidCamAI - Premium Manufacturing Insight Engine"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Networking
  http: ^1.2.0
  
  # UI & Formatting
  google_fonts: ^6.1.0
  flutter_markdown: ^0.6.14
  uuid: ^4.3.3
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    # - assets/images/    # Uncomment if images are added later
